$!/bin/bash

tmp=/tmp/acmereflow.tmp
ns=$HOME/acme/

r() {
        cat $ns/$winid/$1
}
ctl() {
        echo -n "$@" | $ns/$winid/ctl
}

r rdsel | reflow $1 > $tmp

cat $tmp >> $ns/$winid/wrsel

rm -f $tmp
